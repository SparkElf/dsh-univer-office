# Generated build artifacts — never commit these.
# Source lives under src/. Generated lib/ and the runtime build under vendor/
# are intentionally versioned for DSH loading and release installs.
dist/
node_modules/
*.tgz
*.zip
